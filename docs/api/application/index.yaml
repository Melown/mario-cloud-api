#-----------------------------------------------------------------------------
# Application

# list
#listApplications:
#  parameters:
#    - $ref: '../parameters.yaml#/accessToken'
#    - $ref: '../parameters.yaml#/appId'
#  get:
#    tags:
#      - 'Map-Application'
#    summary: 'Returns a list of map(application)s.'
#    responses:
#      200:
#        description: Operation successful.
#        schema:
#          type: object
#          properties:
#            body:
#              $ref: 'definitions.yaml#/MapApplication'

# get
getApplication:
  get:
    parameters:
      - $ref: '../parameters.yaml#/appIdentifier'
    tags:
      - 'Map-Application'
    summary: 'Returns meta-info about a map(application).'
    responses:
      200:
        description: Operation successful.
        schema:
          type: object
          properties:
            body:
              $ref: 'definitions.yaml#/MapApplication'
      404:
        $ref: '../responses.yaml#/404'
